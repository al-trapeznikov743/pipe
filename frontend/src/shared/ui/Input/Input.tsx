import {StyledInput} from './StyledInput';

const Input = () => {
  return (
    <StyledInput
      placeholder={'Enter message'}
      variant={'standard'}
      fullWidth
    />
  );
};

export default Input;